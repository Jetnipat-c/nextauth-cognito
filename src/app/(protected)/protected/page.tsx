import ProtectView from "@/views/protect/Protect";

const Protect = async () => {
  return (
    <div className="flex flex-col items-center justify-center h-screen">
      <ProtectView />
    </div>
  );
};

export default Protect;
